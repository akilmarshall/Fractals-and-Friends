'''
fractals.py
collection of functions that generate fractals
'''

def fractal_canopy(depth, length, angle, ratio):
    tree = [(0, 0), (0, length)]

    def build(tree, depth, length, angle, ratio):
        if n > 0:
            pass
        else:
            return tree

    return tree
